<h1><%= @project.name %></h1>
<ul>
  <% @project.todos.each do |todo| %>
    <li>
        <%= link_to todo.name, todos_path(todo) %>
        <% if todo.completed %>✅<% end %>
    </li>
  <% end %>
</ul>

<%= button_to "Delete Project", project_path(@project), method: :delete %>
<%= link_to "Edit Project", edit_project_path(@project) %>