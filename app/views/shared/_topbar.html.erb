<nav class="bg-white/80 backdrop-blur border-b border-sky-100 shadow-sm">
    <div class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">

        <!-- App name -->
        <%= link_to "☀️ Doable",
            projects_path,
            class: "text-2xl font-bold text-sky-600 hover:text-sky-700 transition" %>

        <!-- Navigation -->
        <div class="flex items-center gap-6">
            <div class="flex items-center gap-6">
                <% active = "underline decoration-2 decoration-sky-400" %>

                <%= link_to "Projects",
                    projects_path,
                    class: "font-medium text-emerald-600 hover:text-emerald-700 transition #{active if controller_name == 'projects'}" %>

                <%= link_to "Todos",
                    todos_path,
                    class: "font-medium text-sky-500 hover:text-sky-600 transition #{active if controller_name == 'todos'}" %>
            </div>
        </div>
    </div>
</nav>